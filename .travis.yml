language: node_js

node_js:
  - 0.12
  - 0.11
  - 0.10
  - iojs

before_install: npm install -g grunt-cli
before_script: grunt
script:
  #nv=$(node -v);
  #if [[ $nv != v0.10.37 ]] ; then
  #  npm test;
  #else
  #  npm run 0.10;
  #fi
