doctype html
html
  head(lang="en")
    title= title
    meta(http-equiv="content-type", content="text/html; charset=UTF-8")
    meta(name="description", content="")
  body
    header
      h1 #{__("Hello")}
      h2 #{__.language('en')}
    .container
      for item, i in __.languages('ja')
        li= item
            | 

    footer